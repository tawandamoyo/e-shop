import React from 'react';
import Button from '../components/Button';

export default function Cart() {

    return (
        <>
            <div>
                {cart.map((item) => {
                    return (
                        
                    )
                })}
            </div>
            <Button></Button>
        </>
    )
}